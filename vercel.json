{
  "version": 2,
  "buildCommand": "pnpm install && pnpm --filter @imedica/backend... build",
  "outputDirectory": "apps/backend/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.js"
    }
  ]
}
